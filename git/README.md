# Git

    └── .gitconfig

## Requirements

- `Git` <<https://git-scm.com>>

## Installation

Before install this section, change `.gitconfig` with your credentials.

    $ rm -f ~/.gitconfig
    $ stow git
